# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'City Weather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for City Weather
# Pods for City Weather
  pod 'SVProgressHUD'
  pod 'SwiftMessages'

  target 'City WeatherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'City WeatherUITests' do
    # Pods for testing
  end

end
